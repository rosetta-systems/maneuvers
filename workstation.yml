---
- hosts: workstation

  pre_tasks:

    - name: Update Repository Index
      dnf:
        update_cache: true
        update_only: true

  roles:
    - workstation
    - ansible
    - git

- import_playbook: common.yml
  vars: 
    CURRENT_HOSTS: workstation

- hosts: workstation
  roles:
    - deploy
