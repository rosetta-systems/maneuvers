---
- hosts: workstation

  pre_tasks:

    - name: Update Repository Index
      dnf:
        update_cache: true
        update_only: true

  roles:
    - workstation
    - git

- import_playbook: common.yml
  vars: 
    CURRENT_HOSTS: workstation
